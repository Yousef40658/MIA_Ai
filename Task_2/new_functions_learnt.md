# pandas.concat()
stacks two data frames vertically by default , can stack them vertically 

# pandas.merge()
didn't know that i can specify multiple keys and how = "inner" will limit the two dfs by the keys of the df with less keys

# pandas.groupby(" ").apply
traditional split-apply-combine 

# np.linalg.eigh 
returns eigenvectors/values in ascending order rather than eig 